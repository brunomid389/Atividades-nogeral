import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(home: Login()));
}

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            CircleAvatar(radius: 35, child: Text("Logo")),
            SizedBox(height: 20),
            SizedBox(
              width: 200,
              child: TextField(decoration: InputDecoration(labelText: "Login")),
            ),
            SizedBox(
              width: 200,
              child: TextField(
                decoration: InputDecoration(labelText: "Senha"),
                obscureText: true,
              ),
            ),
            Checkbox(value: false, onChanged: (v) {}),
            Text("Aceito os termos"),
            ElevatedButton(
              onPressed: () {
                Navigator.push(
                  context,
                  MaterialPageRoute(builder: (context) => Imagens()),
                );
              },
              child: Text("Entrar"),
            ),
          ],
        ),
      ),
    );
  }
}

class Imagens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      drawer: Drawer(),
      appBar: AppBar(backgroundColor: Colors.blue),
      body: GridView.count(
        crossAxisCount: 2,
        padding: EdgeInsets.all(20),
        children: [
          Container(color: Colors.grey),
          Container(color: Colors.grey),
          Container(color: Colors.grey),
          Container(color: Colors.grey),
          Container(color: Colors.grey),
          Container(color: Colors.grey),
        ],
      ),
    );
  }
}
